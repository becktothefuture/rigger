{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rigger Extension",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "watch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/demo-workspace/react-vite"
      ]
    }
  ]
}
